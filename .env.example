# Ключ: https://console.mistral.ai/api-keys
# Скопируйте в .env.local (не сюда!). После изменения: npm run dev
MISTRAL_API_KEY=your_mistral_api_key_here

# Секрет для JWT-сессий (минимум 16 символов, любая случайная строка)
JWT_SECRET=your_random_secret_at_least_16_chars
